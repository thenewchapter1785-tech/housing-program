# UI design notes

- Keep the search flow simple: location, price, and query should be the primary inputs.
- Use a clean layout with tabs or sections for search, saved searches, and profile.
- Prefer a Windows-native look later, but keep the app logic separate from the UI layer.
- Leave room for future ideas such as favorites, filters, and dark mode.
